import React from 'react';

// Este é um componente de placeholder para garantir que o arquivo seja um módulo válido.
// O fluxo principal de autenticação é tratado dentro do AuthModal.
const SignUpModal: React.FC = () => {
    return null;
};

export default SignUpModal;
